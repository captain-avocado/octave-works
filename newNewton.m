function [out] = newNewton(px, x, y)
	for k = 1:length(k),
		out(k) = y(1);
		d = zeros(1, length(x) - 1);
		% d(1) = 
		for i = 1:length(x) - 1,
			for j = 1:i+1,
				for ij = 1:i+1,
					p
				end;
				d(i) += y(j) 
			end;
			for j = 1:i,
				w = w * (px(k) - x(j));
			end;
		end;
	end;
endfunction